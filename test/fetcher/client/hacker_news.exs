defmodule HackerNewsAggregatorEx.Fetcher.HackerNewsTest do
  use ExUnit.Case

  """
    Here the tests were going to make the requests and mock the responses and validate it
  """
  @tag :wip
  test "returns a list of ids from get_top_stories/0" do
    assert true
  end

  @tag :wip
  test "return a Map from the story get_stories/1" do
    assert true
  end
end
