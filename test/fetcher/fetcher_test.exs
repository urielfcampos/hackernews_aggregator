defmodule HackerNewsAggregatorEx.FetcherTest do
  use ExUnit.Case

  """
    On this test, the test is simply going to be if the GenServer is started and has the pid reference to the `Process.send_after()`
  """
  @tag :wip
  test "start and schedule request" do
    assert true
  end
end
