defmodule HackerNewsAggregatorEx.Api.Controllers.TopStoriesTest do
  use ExUnit.Case
  """
  On this test, i'd test the responses to HTTP requests made to /top_stories
  """
  @tag :wip
  test "returns 200 and first 10 stories that exists in memory" do

    assert true
  end

  test "returns 200 and 10 stories that exists in memory based on page number" do

    assert true
  end

  test "returns 200 and empty list when no story found" do
    assert true
  end

end
