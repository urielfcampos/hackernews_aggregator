defmodule HackerNewsAggregatorEx.Api.Controllers.StoryTest do
  use ExUnit.Case

  """
  On this test, i'd test the responses to HTTP requests made to /story/:id
  """

  @tag :wip
  test "returns 200 and story that exists in memory" do

    assert true
  end

  test "returns 404 when no story found" do
    assert true
  end

end
