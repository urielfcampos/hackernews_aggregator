defmodule HackerNewsAggregatorEx.SocketHandler do

  """
  On this test, i'd test the responses to WS requests
  """
  @tag :wip
  test "returns a json text to respond to /ws/top_stories" do
    assert true
  end

  @tag :wip
  test "registers open sockets to register" do
    assert true
  end

  @tag :wip
  test "broadcast to open sockets when stories are updated" do
    assert true
  end
end
