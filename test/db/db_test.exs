defmodule HackerNewsAggregatorEx.DBTest do
  use ExUnit.Case

  """
    Here I would test the DB story retrieval functions
  """

  @tag :wip
  test "return all stories" do
    assert :true
  end

  @tag :wip
  test "return a single story" do
    assert :true
  end

end
